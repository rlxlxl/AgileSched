require('yougile-ui')

// Работа с расписанием — в чате задачи (как Telegram-бот).
// Напишите в чат: /start

try {
  Notifier.success(
    'Расписание РиМ: откройте чат задачи и напишите /start'
  )
} catch (e) {}
